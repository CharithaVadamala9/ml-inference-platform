# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Environment / secrets
.env
.env.local
*.key

# MLflow local store
mlflow.db
mlruns/
mlartifacts/

# Data artifacts (keep the curated eval set, ignore generated/large files)
data/cache/
data/*.tmp
*.faiss
chroma/

# Eval judge disk cache
.cache/

# CI gate verdict comment (generated)
gate_comment.md

# Eval run outputs (generated artifacts; champion.json is the canonical record)
reports/*.json
!reports/.gitkeep

# OS / editor
.DS_Store
.idea/
.vscode/
