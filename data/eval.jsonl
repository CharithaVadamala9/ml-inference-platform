{"id": "q1", "question": "What does the learning rate control during training?", "ground_truth": "The learning rate controls the size of each parameter update; too high can cause divergence, too low makes training slow.", "source_ids": ["learning-rate"]}
{"id": "q2", "question": "How does L1 regularization differ from L2 regularization?", "ground_truth": "L1 penalizes the absolute magnitude of weights and tends to produce sparse solutions with many zero weights, while L2 penalizes the squared magnitude and shrinks weights smoothly toward zero.", "source_ids": ["regularization"]}
{"id": "q3", "question": "What is overfitting and what symptom indicates it?", "ground_truth": "Overfitting is when a model fits noise in the training data and fails to generalize; it shows as low training error but high validation error.", "source_ids": ["overfitting"]}
{"id": "q4", "question": "Why does dropout improve generalization?", "ground_truth": "Dropout randomly zeros activations during training so the network cannot rely on any single neuron, forcing robust, redundant representations.", "source_ids": ["dropout"]}
{"id": "q5", "question": "What is the bias-variance tradeoff?", "ground_truth": "It is the balance between bias (error from a too-simple model that underfits) and variance (error from a too-sensitive model that overfits); increasing complexity lowers bias but raises variance.", "source_ids": ["bias-variance"]}
{"id": "q6", "question": "What is the validation set used for?", "ground_truth": "The validation set is used to tune hyperparameters and select models, separate from the test set which gives the final unbiased performance estimate.", "source_ids": ["train-val-test"]}
{"id": "q7", "question": "What does cross-entropy loss measure and where is it used?", "ground_truth": "Cross-entropy measures the difference between the predicted and true probability distributions and is the standard loss for classification; it is the negative log probability of the correct class.", "source_ids": ["cross-entropy"]}
{"id": "q8", "question": "What does backpropagation compute and how?", "ground_truth": "Backpropagation computes the gradients of the loss with respect to every parameter by applying the chain rule, propagating error backward from output to input.", "source_ids": ["backpropagation"]}
{"id": "q9", "question": "How does batch normalization help training?", "ground_truth": "Batch normalization normalizes layer inputs using mini-batch mean and variance, which stabilizes and speeds up training, allows higher learning rates, and mildly regularizes.", "source_ids": ["batch-norm"]}
{"id": "q10", "question": "Why are Transformers more scalable than recurrent networks?", "ground_truth": "Transformers use self-attention to process all tokens in parallel rather than sequentially, which makes them highly scalable.", "source_ids": ["attention"]}
{"id": "q11", "question": "How does retrieval-augmented generation reduce hallucination?", "ground_truth": "RAG retrieves relevant documents and conditions generation on them, grounding answers in external knowledge without retraining the model.", "source_ids": ["rag"]}
{"id": "q12", "question": "What is stochastic gradient descent and what is one benefit of its noise?", "ground_truth": "SGD estimates the gradient from a small random batch instead of the full dataset, making updates cheaper; the resulting noise can help escape poor local minima.", "source_ids": ["gradient-descent"]}
